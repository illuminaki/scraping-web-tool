<% content_for :title, "Editing sent email" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing sent email</h1>

  <%= render "form", sent_email: @sent_email %>

  <%= link_to "Show this sent email", @sent_email, class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
  <%= link_to "Back to sent emails", sent_emails_path, class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
</div>
